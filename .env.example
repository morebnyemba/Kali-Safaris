# Root Environment Variables - Example Configuration for Docker Compose
# Copy this file to .env and fill in your actual values
# This file is used by docker-compose.yml for container configuration

# --- PostgreSQL Database Configuration ---
DB_ENGINE=django.db.backends.postgresql
DB_NAME=whatsapp_crm_db
DB_USER=crm_user
DB_PASSWORD=your-secure-database-password-here
DB_HOST=db  # Docker service name from docker-compose.yml
DB_PORT=5432

# Note: Individual services may have their own .env files:
# - whatsappcrm_backend/.env.prod for backend configuration
# - whatsapp-crm-frontend/.env for frontend configuration
# See respective .env.example files in those directories for more details
