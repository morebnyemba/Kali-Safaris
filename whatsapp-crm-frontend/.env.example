# Frontend Environment Variables - Example Configuration
# Copy this file to .env and fill in your actual values
# These variables are exposed to your Vite/React application at build time

# --- API Configuration ---
# The base URL for the backend API
# Your frontend will use this to make requests like login, fetching data, etc.
# For local development: http://localhost:8000
# For production: https://backend.yourdomain.com
VITE_API_BASE_URL=http://localhost:8000

# --- WebSocket Configuration ---
# The WebSocket URL for real-time communication (e.g., live chat updates)
# The client-side code will append the specific path (e.g., /ws/conversations/)
# For local development: ws://localhost:8000
# For production: wss://backend.yourdomain.com
VITE_WEBSOCKET_URL=ws://localhost:8000

# Note: All VITE_ prefixed variables are exposed to the client-side code
# Do NOT store sensitive secrets in these variables
